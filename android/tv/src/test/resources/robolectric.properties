# Pin the Robolectric SDK for the screenshot suite. The app targets SDK 36, but
# rendering only uses long-stable Canvas/Compose APIs, so SDK 34 keeps the
# android-all download stable and native-graphics rendering well-trodden.
sdk=34
